{
  "name": "protection-api",
  "version": "0.1.0",
  "private": true,
  "description": "Thin HTTP adapter over the ctxvigil SDK — transport only, zero detection logic (FR-6.6, FR-6.7, architecture §4.4).",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "start": "node src/start.ts",
    "test": "node --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "ctxvigil": "*"
  }
}
