{
  "name": "ctxvigil-cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI for ctxvigil — argument parsing and reporting only; every decision comes from the ctxvigil SDK (FR-6.1, architecture §4.3).",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "bin": {
    "ctxvigil": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.ts\""
  },
  "dependencies": {
    "ctxvigil": "*"
  }
}
