{
  "name": "ctxvigil",
  "version": "0.1.0",
  "description": "Multi-view prompt-injection protection layer for LLM web agents. Explainable, deterministic, offline.",
  "keywords": [
    "prompt-injection",
    "llm-security",
    "ai-agent",
    "accessibility-tree",
    "guardrails"
  ],
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "//publish": "Phase 6 (docs/02_PHASE_PLAN_RISHABH.md §9.3): add a `typescript` devDependency, run `npm run build`, and switch `main`/`exports` from ./src to ./dist before running `npm pack`. Until then this package is workspace-scoped only and must not be claimed as publicly installable (NFR-8).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\"",
    "test:contract": "node --test \"tests/contract.test.ts\""
  },
  "repository": {
    "type": "git",
    "url": "https://example.test/ctxvigil.git"
  }
}
